---
layout: layout
pageable: true
per_page: 10
title: "Пісні за алфавітом"
---

| </pre>
center
  - alphabet.each do |letter|
    = link_to letter, path_to_letter(letter.dup), class: "letter-button btn btn-primary"
